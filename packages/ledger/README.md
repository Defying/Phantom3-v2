# Ledger package

Placeholder for the durable ledger package.

Current bootstrap runtime uses a file-backed state store in `apps/api/src/runtime-store.ts`.
The next milestone should replace that bootstrap persistence with a proper append-only ledger.
